{
  "name": "website",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/website",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "tailwindcss -i assets/css/styles.css -o static/css/styles.css --minify"
          },
          {
            "command": "hugo -d ../../dist/apps/website --gc --minify --baseURL \"${BASE_URL:-/}\""
          }
        ],
        "parallel": false,
        "cwd": "apps/website"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "tailwindcss -i assets/css/styles.css -o static/css/styles.css --watch"
          },
          {
            "command": "hugo server -D"
          }
        ],
        "parallel": true,
        "cwd": "apps/website"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "hugo -d ../../dist/apps/website-preview --gc --minify --baseURL /preview/",
        "cwd": "apps/website"
      }
    }
  }
}
