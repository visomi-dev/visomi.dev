# social-images

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build social-images` to build the library.

## Running unit tests

Run `nx test social-images` to execute the unit tests via [Jest](https://jestjs.io).
